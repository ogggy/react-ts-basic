import { type } from 'os'
import React from 'react'

type CheckList = {
    [key: string]: string
}

const arr = []

const ProgressContext = () => {
  return (
    <div>ProgressContext</div>
  )
}

export default ProgressContext